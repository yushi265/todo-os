# 個人用 TODO 管理アプリ 要件定義書

## 1. 概要

### 1.1 目的

個人で利用するシンプルな TODO 管理Webアプリを構築する。

以下を重視する。

- PC / スマートフォンから利用可能
- 自分以外はアクセス不可
- TODOを素早く登録・管理できる
- TODOの表示順を手動で自由に並び替えられる
- 完了・キャンセル済みTODOの表示／非表示を簡単に切り替えられる
- Cloudflareを中心に構成し、運用コストを抑える
- サーバー管理を不要にする
- 将来的な機能追加が容易な構成とする

---

## 2. システム構成

### 2.1 採用技術

| 分類 | 技術 |
|---|---|
| 言語 | TypeScript |
| フロントエンド | React |
| ビルドツール | Vite |
| API | Hono |
| データ取得・キャッシュ | TanStack Query |
| バリデーション | Zod |
| ORM | Drizzle ORM |
| Database | Cloudflare D1 |
| Runtime | Cloudflare Workers |
| 認証・アクセス制御 | Cloudflare Access |
| パッケージ管理 | pnpm |
| ホスティング | Cloudflare |

### 2.2 アーキテクチャ

```text
PC / Smartphone
       │
       ▼
Cloudflare
       │
       ▼
Cloudflare Access
       │
       │ 認証済みユーザーのみ
       ▼
┌─────────────────────┐
│ Cloudflare Workers  │
│                     │
│ React + Vite        │
│ Hono API            │
└──────────┬──────────┘
           │
           ▼
     Drizzle ORM
           │
           ▼
     Cloudflare D1
```

---

## 3. ユーザー

### 3.1 想定ユーザー

利用者はシステム所有者本人1名のみとする。

マルチユーザー対応は行わない。

### 3.2 認証

Cloudflare Accessを利用する。

アプリケーション側では独自の認証機構を実装しない。

認証方式：

- Cloudflare Access
- One-time PIN
- 指定メールアドレスのみ許可

---

## 4. TODO管理

### 4.1 TODO作成

TODOを新規作成できる。

#### 入力項目

| 項目 | 必須 | 内容 |
|---|---|---|
| タイトル | ○ | TODOタイトル（200文字以内） |
| 説明 | - | 詳細内容 |
| ステータス | ○ | TODOの状態 |
| 優先度 | - | TODOの優先度 |
| 期限 | - | 完了期限（日付のみ、時刻は持たない） |
| タグ | - | 任意のタグ |

作成日時・更新日時・表示順はシステム側で管理する。

新規TODOは、原則として現在の未完了一覧の末尾に追加する。

### 4.2 TODO編集

既存TODOについて以下を変更できる。

- タイトル
- 説明
- ステータス
- 優先度
- 期限
- タグ

### 4.3 TODO削除

TODOを削除できる。

削除前に確認を表示する。

初期バージョンでは物理削除とし、削除後の復元手段は提供しない。個人用アプリでスコープが小さいため、確認ダイアログによる誤操作防止で十分と判断する。誤って削除したくない場合は、削除の代わりに `CANCELED` へのステータス変更を利用できる。

---

## 5. ステータス管理

TODOは以下のステータスを持つ。

```text
TODO
  ↓
IN_PROGRESS
  ↓
DONE

または

TODO / IN_PROGRESS
  ↓
CANCELED
```

| ステータス | 意味 |
|---|---|
| TODO | 未着手 |
| IN_PROGRESS | 作業中 |
| DONE | 完了 |
| CANCELED | キャンセル |

`DONE` と `CANCELED` は終了済みTODOとして扱う。

上記の遷移図は典型的な流れを示す参考図であり、遷移を強制するステートマシンではない。ステータスは4値のいずれへも自由に変更でき（例: `DONE` → `TODO` の差し戻し、`CANCELED` → `IN_PROGRESS` の再開も可能）、API側で遷移可否のバリデーションは行わない。

---

## 6. TODOの手動並び替え

TODO一覧では、TODOの表示順をユーザーが手動で変更できる。

### 6.1 操作方法

PCではドラッグ&ドロップによる並び替えを基本とする。

スマートフォンでは長押し後のドラッグ操作に対応する。

```text
☰ 資料を作る
☰ Cloudflareを調べる
☰ 買い物に行く
```

左側のドラッグハンドルなどを利用して順序を変更できる。

### 6.2 並び順の保存

手動で変更した順番はD1に保存し、再読み込み後も維持する。

各TODOに並び順を表す値を保持する。

```text
todos

id
title
...
sort_order
```

一覧取得時は原則として以下で並べる。

```sql
ORDER BY sort_order ASC
```

### 6.3 フィルター利用時

フィルター適用中も、保存済みの手動並び順を維持する。

例：

```text
全TODO

1. A
2. B
3. C
4. D

タグ「仕事」で絞り込み

1. A
3. C
```

フィルターを解除すると元の並び順に戻る。

### 6.4 ソート機能との関係

手動並び替えをデフォルト表示とする。

明示的に期限や優先度などでソートしている間は、ドラッグ&ドロップによる並び替えを無効化する。

```text
並び順

○ 手動
○ 期限
○ 優先度
○ 作成日時
○ 更新日時
```

手動並び順へ戻すと、保存済みの順序を再表示する。

---

## 7. 優先度

TODOには以下の優先度を設定できる。

| 優先度 | 意味 |
|---|---|
| HIGH | 高 |
| MEDIUM | 中 |
| LOW | 低 |

未指定も許可する。

---

## 8. タグ管理

TODOには複数のタグを設定できる。

タグについて以下を行える。

- 作成
- 編集
- 削除
- TODOへの複数タグ設定

タグ名は一意とする（重複するタグ名は作成できない）。

タグを削除した場合、そのタグを持つ全TODOから当該タグの関連付けが解除される（TODO自体は削除されない）。

---

## 9. TODO一覧

TODOを一覧表示する。

表示項目：

- タイトル
- ステータス
- 優先度
- 期限
- タグ
- ドラッグハンドル

期限切れのTODOは視覚的に判別できるようにする。

---

## 10. 終了済みTODOの表示切り替え

TODO一覧には、終了済みTODOを表示するかどうかを切り替えるトグルを設ける。

終了済みTODOとは以下を指す。

```text
DONE
CANCELED
```

### 10.1 初期状態

デフォルトでは終了済みTODOを非表示とする。

```text
[ ] 完了・キャンセル済みを表示
```

トグルをONにすると、

```text
[x] 完了・キャンセル済みを表示
```

`DONE` および `CANCELED` のTODOも一覧へ表示する。

### 10.2 表示イメージ

```text
TODO

☰ Cloudflareを調べる
☰ 買い物に行く
☰ 資料を作る

────────────────────

[ ] 完了・キャンセル済みを表示
```

ONの場合：

```text
TODO

☰ Cloudflareを調べる
☰ 買い物に行く
☰ 資料を作る

────────────────────

[x] 完了・キャンセル済みを表示

✓ 要件定義を書く
× 古い調査タスク
```

終了済みTODOは未完了TODOと視覚的に区別する。

終了済みTODOは手動並び替え（ドラッグ&ドロップ）の対象外とし、更新日時の降順で固定表示する。上記表示イメージの通り、終了済みTODOにはドラッグハンドル（☰）を表示しない。

### 10.3 表示設定の保持

トグルの状態はブラウザ側で保持する。

初期実装では `localStorage` を利用する。

```ts
localStorage.setItem(
  "showCompletedTodos",
  String(showCompletedTodos),
);
```

この設定はユーザー固有データとしてD1に保存する必要はない。

---

## 11. フィルター

以下の条件でTODOを絞り込める。

### ステータス

```text
すべて
TODO
IN_PROGRESS
DONE
CANCELED
```

### 優先度

```text
HIGH
MEDIUM
LOW
```

### タグ

指定タグを持つTODOのみ表示する。

### 期限

```text
今日
期限切れ
期限なし
```

「今日」「期限切れ」の判定は日本時間（Asia/Tokyo）基準で行う。

複数条件を組み合わせて利用可能とする。

---

## 12. ソート

TODOを以下の並び順で表示できる。

- 手動
- 作成日時
- 更新日時
- 期限
- 優先度

デフォルトは `手動` とする。

日時・期限・優先度については昇順・降順を選択可能とする。

---

## 13. 検索

TODOのタイトルおよび説明を対象としてキーワード検索できる。

検索中も保存済みの手動並び順を維持する。

---

## 14. 画面構成

### 14.1 TODO一覧

```text
┌────────────────────────────────┐
│ TODO                  [+ 追加] │
├────────────────────────────────┤
│ 🔍 [________________________]  │
│                                │
│ Status [▼] Tag [▼]             │
│ 並び順 [手動 ▼]                │
│                                │
│ [ ] 完了・キャンセル済みを表示 │
├────────────────────────────────┤
│ ☰ Cloudflareを調べる           │
│   開発  HIGH   今日            │
│                                │
│ ☰ 買い物                       │
│   個人  LOW    8/20            │
│                                │
│ ☰ 資料を作る                   │
│   仕事  MEDIUM                 │
└────────────────────────────────┘
```

---

## 15. レスポンシブ対応

以下をサポートする。

- PC
- タブレット
- スマートフォン

スマートフォンではドラッグ操作が誤発火しないよう、TODO全体ではなくドラッグハンドルからの並び替えを基本とする。

```text
☰ TODOタイトル
```

---

## 16. データモデル

### todos

```text
todos

id
title
description
status
priority
due_date
sort_order
created_at
updated_at
```

`sort_order` はTODOの手動表示順を保持する。

### tags

```text
tags

id
name
created_at
updated_at
```

### todo_tags

```text
todo_tags

todo_id
tag_id
```

`todo_id`・`tag_id` はそれぞれ `todos.id`・`tags.id` への外部キーとし、`ON DELETE CASCADE` を設定する。TODOまたはタグが削除された場合、対応する `todo_tags` の関連付けは自動的に削除される（TODO本体・タグ本体は連動して削除されない）。

---

## 17. API

### TODO

```text
GET    /api/todos
POST   /api/todos
GET    /api/todos/:id
PATCH  /api/todos/:id
DELETE /api/todos/:id
```

### 並び替え

TODOの表示順をまとめて更新するAPIを提供する。

```text
PATCH /api/todos/reorder
```

`todoIds` には、フィルター適用の有無にかかわらず、対象ユーザーが保持する全TODOのIDを過不足なく含める。フィルター表示中の並び替えも、クライアント側でフィルター対象外のTODOを含めた完全なリストへ再構成してから送信する。

リクエスト例：

```json
{
  "todoIds": [
    12,
    5,
    8,
    3
  ]
}
```

サーバー側で受け取った順序を `sort_order` として保存する。

処理イメージ：

```ts
for (const [index, todoId] of todoIds.entries()) {
  await db
    .update(todos)
    .set({
      sortOrder: index,
    })
    .where(eq(todos.id, todoId));
}
```

実装時は可能な範囲で一括更新またはトランザクション相当の処理を利用する。

---

## 18. バリデーション

API入力値はZodで検証する。

```ts
const reorderTodosSchema = z.object({
  todoIds: z.array(z.number().int().positive()),
});
```

並び替え対象に重複IDが含まれないことも検証する。

また、`todoIds` が対象ユーザーの全TODO ID集合と過不足なく一致することを検証し、不一致の場合はエラーとする。

---

## 19. セキュリティ

### 19.1 アクセス制御

Cloudflare Accessにより指定メールアドレス以外からのアクセスを拒否する。

### 19.2 API保護

`/api/*` もCloudflare Accessの保護対象とする。

### 19.3 入力値検証

更新系APIではZodによる検証を実施する。

### 19.4 SQL Injection対策

Drizzle ORMまたはPrepared Statementを利用する。

---

## 20. 初期スコープ

初期リリースでは以下を実装する。

- TODO作成
- TODO編集
- TODO削除
- TODO一覧
- ステータス管理
- `CANCELED` ステータス
- 優先度管理
- 期限管理
- タグ管理
- 検索
- フィルター
- 手動並び替え
- 並び順の永続化
- 完了・キャンセル済みTODOの表示トグル
- レスポンシブ対応
- Cloudflare Accessによるアクセス制御
- Cloudflare D1へのデータ保存

---

## 21. スコープ外

初期リリースでは以下を対象外とする。

- 複数ユーザー
- ユーザー登録
- 独自ログイン機能
- TODO共有
- コメント
- 添付ファイル
- 通知
- 外部サービス連携
- ネイティブアプリ
- オフライン対応

---

## 22. 採用構成まとめ

```text
                Internet
                    │
                    ▼
          ┌──────────────────┐
          │ Cloudflare Access│
          └────────┬─────────┘
                   │
                   ▼
         ┌────────────────────┐
         │ Cloudflare Workers │
         │                    │
         │ React + Vite       │
         │ Hono               │
         └─────────┬──────────┘
                   │
              Drizzle ORM
                   │
                   ▼
             ┌──────────┐
             │    D1    │
             │ SQLite   │
             └──────────┘
```

基本方針は **Cloudflare上で完結する、小さくシンプルな個人用TODO管理アプリ** とする。

TODO一覧のデフォルト表示は手動並び順とし、日常的な整理をドラッグ&ドロップで行えることを重視する。

また、通常利用時は未完了TODOへ集中できるよう、`DONE` と `CANCELED` はデフォルト非表示とし、必要な場合のみトグルで表示する。
