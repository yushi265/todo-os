# TODOリスト

> 思いついたときに書くバックログ。着手する時は `docs/spec/<ユニット名>/` で正式にspec化する（AI-DLCフロー）。

## 未完了

### 優先度未設定

- [ ] faviconいい感じのやつを追加する。
- [ ] shadcn/uiを全面的に適用する。
- [ ] ステータスを、一覧でのステータスの表示を、今の完了とか中止と同じような表示にする。左側にアイコンを表示。
- [ ] ステータスの変更やタイトルの変更など、何らかの変更をした場合に、トーストを出して元に戻せるようにボタンを配置する。
- [ ]

### 高

- [x] sm以上のTODO一覧カードで、ステータス・優先度・期限とタグを同じ一行に配置する（sm未満は現行の行分けを維持する）。
- [x] sm以上のTODO一覧カードで、タイトル行の上下・周辺の余白を現状より狭める（sm未満は現行の余白を維持する）。
- [x] TODO一覧画面の主要操作、キーボード操作、フォーカス表示、名称、色コントラストをW3C WCAG 2.1 AAに適合させ、検証結果を確認できるようにする。
- [x] 手動並び順時に、完了（DONE）・中止（CANCELED）のTODOもドラッグ&ドロップで並び替えられるようにする。
- [x] 手動並び順時に、ドラッグ&ドロップの開始操作を6点リーダーだけでなくTODOカード全体から行えるようにする。
- [x] CodexやClaude Codeから、アプリの認証境界を迂回せず認証済みリクエストとしてTODOを追加できる入力経路を用意する。

### 中

- [ ] ステータスのバッジの色合いをテーマカラーに関わらず固定する。未着手はグレー、進行中は青、完了は緑にする。
- [ ] 説明が入力されているTODOを、一覧上でアイコンなどにより判別できるようにする。
- [ ] タグごとに一覧を切り替えられるUIを追加し、既存の単一タグフィルターとの差分を定義する。

### 低

- [ ] 一覧の優先度を表示するときに、「優先度」という文字を省略し、値だけを表示する。
- [ ] TODOをドラッグ&ドロップするとき、対象行が浮いて見えるアニメーションを追加する。

## 完了

- [x] 検索フォーム・フィルター・並び順のUIを実装・改善する。
- [x] クイックでタスクを追加できるようにする。
- [x] 期限切れ・本日期限・近日のTODOを、一覧上で絵文字により判別できるようにする。
- [x] UI全体に、モーダル・TODO行・状態変更などを対象とした控えめなアニメーションを追加する。
- [x] shadcn/uiの導入効果を確認し、採用するコンポーネントだけ段階的に置き換える。
- [x] 複数のカラーテーマを用意し、設定画面から切り替えられるようにする。
