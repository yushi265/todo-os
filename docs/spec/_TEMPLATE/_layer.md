# <TICKET>: <レイヤー名> 詳細設計

> **レイヤー別 spec の汎用雛形**。この `_layer.md` を雛形に、関与するレイヤーごとに `<layer>.md` を作る
> （todo-os は `service.md`（Hono API + Drizzle/D1。data 層を統合）/ `ui.md`（React）の2層。詳細は [architecture.md](../../architecture.md)）。
> **そのレイヤーが関与しない場合はファイルを作らない**。単一レイヤーで完結する変更（CLI・ライブラリ等）なら 1 ファイルでよい。
> 規約: プロジェクトの当該レイヤーの規約 docs（あれば）に従う。

## 担保 AC（[index.md](./index.md) の AC からの引用）

> このレイヤーで担保する AC を番号と引用文で記す。引用文は [index.md](./index.md) の AC とリテラル一致させる
> （[spec-conformance-reviewer](../../../.claude/agents/spec-conformance-reviewer.md) が機械チェック）。

- **AC-N**: <index.md の AC-N の文言をそのまま引用>

## このレイヤーが公開する契約（外部インターフェース）

> このレイヤーが上位レイヤー・利用者へ公開する IF の**確定値**を書く（レイヤー間契約の正本）。
> 形式はレイヤーによる（例: データ層 = スキーマ／テーブル定義、サービス層 = API パス・リクエスト／レスポンス、
> 表示層 = 画面パス・ルーティング）。名前・型・パス・制約・アクセス制御を具体値で確定する。

| 操作 | 名前 / パス | 入出力・型・制約 | 認証・アクセス制御 | 用途 |
|------|------------|-----------------|-------------------|------|
| 追加 / 変更 / 削除 | <IF 名 or パス> | <スキーマ・JSON・型の具体値> | <要 / 不要・権限> | <用途> |

<スキーマ定義・リクエスト／レスポンス等の具体例が必要なら、確定値をコードブロックで示す>

## このレイヤーが依存する下位の契約（呼び出す相手）

> このレイヤーが依存する下位レイヤー・外部サービスの IF（呼び出す相手）。無ければこの節ごと省略。

- 呼び出す相手: <下位レイヤーの IF 名（当該レイヤーの `<layer>.md` を参照）>
- 受け渡し: <認証情報・識別子・リクエストヘッダー等の引き継ぎ>

## 実装配置

> このレイヤーで触るファイル・モジュールの所在（プロジェクトのディレクトリ構造に沿って）。

- <触るファイル・モジュールの所在>

## UI/UX 方針（表示層が関与する場合のみ）

> 表示層（UI）が関与する場合のみ。関与しなければこの節ごと省略。
> 設計のアプローチ（Why・採用案／却下案）は [index.md](./index.md) の「判断根拠 / 未決事項」が正本。

- **画面フロー / 導線**: <主要画面の遷移・ユーザー導線>
- **主要操作とフィードバック**: <主要アクションと、成功／失敗の見せ方>
- **状態設計（出し分け）**: 初期・入力中 / ローディング / 空（0 件）/ エラー（4xx/5xx・権限なし）/ 成功 の各表示
- **既存デザインシステムとの整合**: <再利用するコンポーネント・トークン。新規追加は Rule of Three（[simplicity.md](../../../.claude/rules/simplicity.md)）を満たすか>

### レスポンシブ / アクセシビリティ（表示層は必須・空通過禁止）

> 表示層 spec（例: `ui.md`）ではこの見出しを必須にする（engine の spec-sections sensor が見出しの有無を検査する）。

- <対象端末・主対象ブレークポイント・タブレット/スマホ方針・a11y 最低限。プロジェクトの UI 規約に従う>

## 異常系挙動

> [index.md](./index.md) の横断エラーサマリの当該シナリオについて、本レイヤー視点の詳細を書く。
> エラーコード体系・ログ要件はプロジェクトのエラー／ログ規約（あれば）に従う。

| シナリオ | 本レイヤーの挙動（エラーコード・レスポンス／表示・ログ） |
|---|---|
| <例: 未認証 / 入力不正 / 下位レイヤー障害> | <具体的な挙動> |

## テストケース（技法注記付き）

> [tdd-cycle](../../../.claude/skills/tdd-cycle/SKILL.md) の技法マッピングに沿って導出。
> Gate 2（spec 承認）で人間に提示するテスト設計。Stage 3 以降に発見したケースは progress.md に追記する。
> テスト実行コマンド・カバレッジ規約はプロジェクトの当該レイヤーの規約に従う。

- [<技法>] <ケース（入力・条件 → 期待結果）>
- [境界値] <例: 入力長 100 ちょうど → 成功 / 101 → 失敗>
- [デシジョンテーブル] <例: 認証あり × 権限あり = 成功 / 認証あり × 権限なし = 拒否 / 認証なし = 拒否>
