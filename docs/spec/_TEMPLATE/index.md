# <TICKET>: <機能名>

> 薄い実装 spec の入口。**読み手は人間（Gate 1/2/3 承認者）**。AI 実装エージェントは
> レイヤー別 `.md`（`_layer.md` を雛形に、関与レイヤーごとに `<layer>.md` を作る。例: `data.md` / `service.md` / `ui.md`）を読む。
> spec は**チケット単位のディレクトリ**で管理し、レイヤー詳細は同ディレクトリの各 .md に分割する。
> **関与しないレイヤーのファイルは作らない**。
> 設計判断（Why・採用案/却下案・トレードオフ）は本書「判断根拠 / 未決事項」、UI/UX 方針は表示層レイヤー .md の「UI/UX 方針」が正本。
> ディレクトリ名: `docs/spec/<TICKET>-<kebab-case-feature>/`（例: `<TICKET>-csv-export/`）

## 概要

<この実装で何を実現するか。1〜3 行>

## 対象範囲

- 対象レイヤー: <関与するレイヤー（例: データ / サービス / 表示）。各レイヤーの詳細はリンク。表示層が関与する時の UI/UX 方針は当該レイヤー .md の「UI/UX 方針」>
  - <関与レイヤーごとに `<layer>.md`（雛形: [_layer.md](./_layer.md)）を作りリンクする。例: `data.md` / `service.md` / `ui.md`>
- 対象ドメイン: <該当ドメイン>
- 対象外（やらないこと）: <スコープ外を明記。YAGNI>

## ユニット計画

> チケットが**複数のユニット（Unit of Work）**に割れる場合に記録する（[import-ticket](../../../.claude/skills/import-ticket/SKILL.md) の申し送りを受ける）。用語は [glossary.md](../../ai-dlc/glossary.md)。
> 各ユニットはデータ層→表示層を貫く 1 機能（1〜数ボルトで仕上げる）。**PR の単位は固定しない**（Gate 3 で人間が決める）。単一ユニットで完結するなら「単一ユニット」とだけ書く。

| # | ユニット | 含む AC | 依存 | 状態 |
|---|---------|--------|------|------|
| 1 | <ユニット名> | AC-1,2 | — | <未着手 / 進行中 / 完了> |

## 受け入れ基準（AC）

> **AC は本書が正本**。各レイヤー .md（`<layer>.md`）は
> 「担保 AC 番号 + 引用文」を持つ（[spec-conformance-reviewer](../../../.claude/agents/spec-conformance-reviewer.md) が引用整合を機械チェック）。
> **AC は番号付け必須**（`AC-1` / `AC-2` ...）。レイヤー .md からの引用は AC 番号で参照する。

- [ ] **AC-1**: <観測可能な完了条件（テストで判定できる粒度。正常系・異常系・境界値を含める）>
- [ ] **AC-2**: <観測可能な完了条件>

## アーキテクチャ / レイヤー間フロー

<関与レイヤー間のシーケンス・データフローの要約図。レイヤー間インターフェースの確定値は各レイヤーファイルが正本>

## エラー・ログ方針（横断サマリ）

> シナリオごとに各レイヤーの挙動カテゴリ（エラーコード / ステータス / 表示の種別）の**サマリ**を表で確定する。
> レイヤー視点の詳細（具体的なメッセージ・遷移先・ログ内容）は各レイヤー .md の「異常系挙動」節が正本。

| シナリオ | <レイヤー1> | <レイヤー2> | 表示層の挙動 |
|---|---|---|---|
| <例: 未認証アクセス> | — | — | <挙動カテゴリ> |

## テスト戦略

> 基準は [.claude/rules/testing.md](../../../.claude/rules/testing.md) の「テスト種別の分担」に従う
> （正常系フロー・異常系・境界値・分離保証 = レイヤー内結合 / ロジック・変換 = 単体）。
> **ケース詳細は各レイヤー .md の「テストケース」節を参照**（本書はマトリクスのみ）。

| AC | 単体 | レイヤー内結合 |
|----|------|--------------|
| AC-1 | <対象 or —> | <対象 or —> |

## 既存実装との関係（再利用 / 差分 / 衝突）

<関連する既存機能・既存 schema・共有契約と、本実装の関係を簡潔に書く。
 再利用するもの / 既存との差分 / 衝突・依存（共有契約・共有コンポーネント含む）を洗い出す。
 着手前に create-spec の現実装調査で埋める。新規実装で既存資産との関係が薄ければ省略（YAGNI）。>

## 実装に効く制約

<このチケットで守るべき横断制約を箇条書き。レイヤー固有の制約は各レイヤーファイルへ>

## 判断根拠 / 未決事項

> 設計のアプローチ（Why）の正本。**採用したアプローチ・却下した代替案とその理由・受け入れるトレードオフ／リスク**を、
> 設計判断ごとに 1〜数行で書く。代替案が実質ない trivial な判断は理由だけで足りる（YAGNI・表は作らない）。

- <採用したアプローチと、その理由（必要なら却下した代替案・トレードオフを併記）>
- <この spec で行ったその他の設計判断と理由>
- <人間に確認が必要な未決事項（実装着手前にゼロにする）>
