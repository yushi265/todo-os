# pwa-installable AI-DLC 振り返り学習ノート（retro note）

## メタ

- ticket: pwa-installable
- 機能概要: todo-osをインストール可能なPWAにし、静的アプリシェルをオフライン表示できるようにする。
- Stage宣言の結果: Stage 0+1〜8を実行。Stage 2は複数のブラウザ資産と起動契約を固定するため実行。
- トークン実測: 未計測
- 着手日 / 完了日: 2026-08-18 / 2026-08-18

## 各Stageの気づき（材料・軽量）

| Stage | 気づき（摩擦・想定外・判断） |
|-------|------------------------------|
| 1 要件整理＋Stage宣言 | PWA化はインストール対応とオフラインデータ同期で設計コストが大きく異なるため、対象をインストール可能な静的シェルに限定した。 |
| 2 spec作成 | 既存Vite/Cloudflare Assetsの`public/`コピー経路を使うことで、Vite PWAプラグインとwrangler設定変更を避けられる。 |
| 3+4 TDD | Manifest・アイコン・Service Worker登録の契約を先にテストし、登録対象外の開発環境/API/非GETを明示的に固定できた。 |
| 5 静的解析 | Wranglerのログ書き込みはサンドボックス制約で警告になったが、権限付きの本番ビルドは警告なく完了した。 |
| 6 セルフレビュー | 既存のユーザー変更を保持しつつ、PWA対応以外のAPI・DB・インフラ設定へ変更が広がっていないことを確認した。 |
| 8 成果提示 | 全テスト・referee-check・buildの完了結果をGate 3提示用に整理した。 |

## 振り返り（KPT）

### Keep（効いた・次も続ける）

- 既存のfavicon、配色、ビルド経路を再利用して変更範囲を限定する。

### Problem（詰まった・摩擦・想定外）

- `[tooling]` PWAアイコンのPNG生成は既存のリポジトリ資産に含まれていないため、生成手段の確認が必要だった。

### Try（次ボルト以降でフローをこう変える）

- アイコン生成物のサイズとビルド成果物へのコピーをテストで固定する。

## フロー改善アクション

| Try | 還流先 | ステータス |
|-----|--------|-----------|
| PWAアイコン生成の標準手段を必要に応じて記録する | docs/ai-dlc/codekb/shared.md | 未対応 |
